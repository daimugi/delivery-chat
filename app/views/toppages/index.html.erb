<div class="text-center">
  <h1>DeliveryChat</h1>
  <img src="/assets/toppage.jpg" alt="/">
</div>
<% if logged_in? %>
  <%= render 'users/users', users: @users %>
<% end %>

<%= render 'toppages/explanation' %>

<%= render 'toppages/how_to' %>